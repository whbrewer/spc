To run all unit tests:

    python test_unit.py

To run all functional tests:

    ln -s ../views .
    ln -s ../static .
    ln -s ../views .
    mkdir user_data
    mkdir apps
    ../sp go >& log &
    python test_func.py

To create a new/fresh database:

    rm -f db/*
    ../sp init
